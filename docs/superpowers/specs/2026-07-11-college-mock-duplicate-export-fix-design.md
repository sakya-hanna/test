# 学院端 Mock 重复导出修复设计

## 问题

`src/mock/col.js` 同时声明了两套 `colCompanyReviewList` 和
`colCompanyStatusMap`，导致 Vite 报重复导出并终止生产构建。

## 决策

删除文件开头的旧版企业审核数据，保留后半部分的数据集。保留版本使用
`待初审` 状态和 `phone` 字段，与 `src/views/col/CompanyReview.vue` 当前消费契约一致。

本次不合并两套演示数据、不重命名导出，也不调整学院审核业务流程。

## 回归保护

先增加一个会失败的契约测试，验证：

- 学院 mock 模块可以成功导入；
- 企业审核列表使用页面所需的 `待初审` 状态；
- 每条企业记录提供页面读取的 `phone` 字段。

随后删除旧版重复声明，使针对性测试、全量测试和生产构建全部通过。
